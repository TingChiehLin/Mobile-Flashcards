import DOCKS from '../../../utils/Api';

const initialState = {
    availableDecks:[],
    question:[]
};

const decksReducer = (state = initialState, action) => {
    
    
    return state;
};

export default decksReducer;